{
  "name": "orbit-claude-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local account and session manager for the official Claude Code CLI. No inference proxy.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
