{
  "name": "claude-runway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local native-session launcher. Official Claude Code owns authentication and credentials.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
